import { Models } from "@triply/utils";
import App from "./App";
export default class Service {
    private _app;
    private _info?;
    private datasetPath;
    private datasetNameWithOwner;
    private name;
    private type;
    private reasoner?;
    constructor(conf: {
        app: App;
        name: string;
        datasetPath: string;
        datasetNameWithOwner: string;
        type: Models.ServiceType;
        reasoner?: Models.JenaReasoners;
    });
    getInfo(refresh?: boolean): Promise<Models.Service>;
    isUpToDate(): Promise<boolean>;
    delete(): Promise<void>;
    create(): Promise<this>;
    private waitUntilRunning;
    update(): Promise<void>;
    private _getServicePath;
}
