import Dataset from "./Dataset";
import { Models } from "@triply/utils";
import User from "./User";
export interface AppConfig {
    token?: string;
    url?: string;
}
export default class App {
    private _config;
    private _info?;
    private constructor();
    private validateConf;
    getApiInfo(): Promise<Models.ClientConfig>;
    getAccount(accountName?: string): Promise<import("./Account").Account>;
    getUser(accountName?: string): Promise<User>;
    getOrganization(accountName: string): Promise<import("./Org").default>;
    getDataset(accountName: string, dsName: string): Promise<Dataset>;
    isCompatible(minimumVersion: string): Promise<boolean>;
    static get(conf?: AppConfig | string): App;
}
