prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#>
select ?label ?n {
  {
    select ?c (sample(?label) as ?label) (count(distinct ?i) as ?n) {
      ?i a ?c.
      ?c rdfs:label ?label.
    }
    order by desc(?n)
  }
}